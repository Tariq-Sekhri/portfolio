# Portfolio

**Live site: https://tariq-sekhri.github.io/portfolio/**

If you see a blank page or 404: in this repo go to **Settings → Pages** and set **Source** to the **gh-pages** branch (not `master`). Then open the URL above.

---

React + Vite + TypeScript + Tailwind. Build: `npm run build`. Deploy: push to `master`; the workflow builds and updates `gh-pages`.
